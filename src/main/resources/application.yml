spring:
  #swagger
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  #jpa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: localhost
    username: root
    password: 1q2w3e4r
  profiles:
    active: test

#Encoding
server:
  servlet:
    encoding:
      force-response: true

#JWT token
jwt:
  token:
    secret: key