spring:
  #swagger ??
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  #jpa ??
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: localhost
    username: root
    password: 1q2w3e4r

#???
server:
  servlet:
    encoding:
      force-response: true

#token ? ??
jwt:
  token:
    secret: key